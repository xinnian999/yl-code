---
alwaysApply: true
---

1. 这个项目all in bun ， npm相关操作都必须使用bun
2. 不要写多重嵌套，多重三目运算，这样不方便维护，超过2重嵌套必须拆分优化
3. 单文件代码尽量不超过两百行，否则考虑拆分
4. 所有的函数、类、接口、类型等都必须有注释，注释必须中文
5. 这个项目后面会接多端，比如web端或者vscode插件。所以agent逻辑层必须保持独立，不要依赖于具体的端。
6. 写新功能时，agent逻辑尽量写到core里保持独立可复用，cli端只调用方法处理交互。